# Resend MCP Server - MCP Hints Overlay
#
# This overlay adds MCP-specific annotations to help AI assistants
# understand tool behavior and safety characteristics.
#
# Annotations:
# - readOnlyHint: true for operations that don't modify state
# - destructiveHint: true for operations that delete/replace data
# - idempotentHint: true for operations safe to retry
# - openWorldHint: true for operations that communicate externally (send emails)
#
# Usage: Applied during merge:spec to produce openapi/merged.yaml

overlay: 1.0.0
info:
  title: Resend MCP Hints Overlay
  version: 1.0.0

actions:
  # =============================================================================
  # Email Operations
  # =============================================================================
  # POST /emails - Sends actual emails to recipients
  - target: "$.paths['/emails'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: false
          openWorldHint: true

  # GET /emails - List emails (read-only)
  - target: "$.paths['/emails'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # GET /emails/{email_id} - Get single email (read-only)
  - target: "$.paths['/emails/{email_id}'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # PATCH /emails/{email_id} - Update scheduled email
  - target: "$.paths['/emails/{email_id}'].patch"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # POST /emails/{email_id}/cancel - Cancel scheduled email
  - target: "$.paths['/emails/{email_id}/cancel'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # POST /emails/batch - Sends batch of emails (up to 100)
  - target: "$.paths['/emails/batch'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: false
          openWorldHint: true

  # GET /emails/{email_id}/attachments - List attachments (read-only)
  - target: "$.paths['/emails/{email_id}/attachments'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # GET /emails/{email_id}/attachments/{attachment_id} - Get attachment (read-only)
  - target: "$.paths['/emails/{email_id}/attachments/{attachment_id}'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # =============================================================================
  # Receiving Emails Operations
  # =============================================================================
  # GET /emails/receiving - List received emails (read-only)
  - target: "$.paths['/emails/receiving'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # GET /emails/receiving/{email_id} - Get received email (read-only)
  - target: "$.paths['/emails/receiving/{email_id}'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # GET /emails/receiving/{email_id}/attachments - List received attachments (read-only)
  - target: "$.paths['/emails/receiving/{email_id}/attachments'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # GET /emails/receiving/{email_id}/attachments/{attachment_id} - Get received attachment (read-only)
  - target: "$.paths['/emails/receiving/{email_id}/attachments/{attachment_id}'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # =============================================================================
  # Domain Operations
  # =============================================================================
  # GET /domains - List domains (read-only)
  - target: "$.paths['/domains'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # POST /domains - Create domain
  - target: "$.paths['/domains'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: false
          openWorldHint: false

  # GET /domains/{domain_id} - Get domain (read-only)
  - target: "$.paths['/domains/{domain_id}'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # PATCH /domains/{domain_id} - Update domain
  - target: "$.paths['/domains/{domain_id}'].patch"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # DELETE /domains/{domain_id} - Delete domain (destructive)
  - target: "$.paths['/domains/{domain_id}'].delete"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: true
          idempotentHint: true
          openWorldHint: false

  # POST /domains/{domain_id}/verify - Verify domain
  - target: "$.paths['/domains/{domain_id}/verify'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # =============================================================================
  # API Key Operations (will be excluded)
  # =============================================================================
  # GET /api-keys - List API keys (read-only)
  - target: "$.paths['/api-keys'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # POST /api-keys - Create API key
  - target: "$.paths['/api-keys'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: false
          openWorldHint: false

  # DELETE /api-keys/{api_key_id} - Delete API key (destructive)
  - target: "$.paths['/api-keys/{api_key_id}'].delete"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: true
          idempotentHint: true
          openWorldHint: false

  # =============================================================================
  # Template Operations
  # =============================================================================
  # GET /templates - List templates (read-only)
  - target: "$.paths['/templates'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # POST /templates - Create template
  - target: "$.paths['/templates'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: false
          openWorldHint: false

  # GET /templates/{id} - Get template (read-only)
  - target: "$.paths['/templates/{id}'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # PATCH /templates/{id} - Update template
  - target: "$.paths['/templates/{id}'].patch"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # DELETE /templates/{id} - Delete template (destructive)
  - target: "$.paths['/templates/{id}'].delete"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: true
          idempotentHint: true
          openWorldHint: false

  # POST /templates/{id}/publish - Publish template
  - target: "$.paths['/templates/{id}/publish'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # POST /templates/{id}/duplicate - Duplicate template
  - target: "$.paths['/templates/{id}/duplicate'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: false
          openWorldHint: false

  # =============================================================================
  # Audience Operations
  # =============================================================================
  # GET /audiences - List audiences (read-only)
  - target: "$.paths['/audiences'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # POST /audiences - Create audience
  - target: "$.paths['/audiences'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: false
          openWorldHint: false

  # GET /audiences/{id} - Get audience (read-only)
  - target: "$.paths['/audiences/{id}'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # DELETE /audiences/{id} - Delete audience (destructive)
  - target: "$.paths['/audiences/{id}'].delete"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: true
          idempotentHint: true
          openWorldHint: false

  # =============================================================================
  # Contact Operations
  # =============================================================================
  # GET /audiences/{audience_id}/contacts - List contacts (read-only)
  - target: "$.paths['/audiences/{audience_id}/contacts'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # POST /audiences/{audience_id}/contacts - Create contact
  - target: "$.paths['/audiences/{audience_id}/contacts'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: false
          openWorldHint: false

  # GET /audiences/{audience_id}/contacts/{email} - Get contact by email (read-only)
  - target: "$.paths['/audiences/{audience_id}/contacts/{email}'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # PATCH /audiences/{audience_id}/contacts/{email} - Update contact by email
  - target: "$.paths['/audiences/{audience_id}/contacts/{email}'].patch"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # DELETE /audiences/{audience_id}/contacts/{email} - Delete contact by email (destructive)
  - target: "$.paths['/audiences/{audience_id}/contacts/{email}'].delete"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: true
          idempotentHint: true
          openWorldHint: false

  # GET /audiences/{audience_id}/contacts/{id} - Get contact by ID (read-only)
  - target: "$.paths['/audiences/{audience_id}/contacts/{id}'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # PATCH /audiences/{audience_id}/contacts/{id} - Update contact by ID
  - target: "$.paths['/audiences/{audience_id}/contacts/{id}'].patch"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # DELETE /audiences/{audience_id}/contacts/{id} - Delete contact by ID (destructive)
  - target: "$.paths['/audiences/{audience_id}/contacts/{id}'].delete"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: true
          idempotentHint: true
          openWorldHint: false

  # =============================================================================
  # Contact Segments Operations
  # =============================================================================
  # GET /audiences/{audience_id}/contacts/{id}/segments - List contact segments (read-only)
  - target: "$.paths['/audiences/{audience_id}/contacts/{id}/segments'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # POST /audiences/{audience_id}/contacts/{id}/segments - Add contact to segment
  - target: "$.paths['/audiences/{audience_id}/contacts/{id}/segments'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: false
          openWorldHint: false

  # DELETE /audiences/{audience_id}/contacts/{id}/segments/{segment_id} - Remove from segment (destructive)
  - target: "$.paths['/audiences/{audience_id}/contacts/{id}/segments/{segment_id}'].delete"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: true
          idempotentHint: true
          openWorldHint: false

  # =============================================================================
  # Contact Topics Operations
  # =============================================================================
  # GET /audiences/{audience_id}/contacts/{id}/topics - Get contact topics (read-only)
  - target: "$.paths['/audiences/{audience_id}/contacts/{id}/topics'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # PATCH /audiences/{audience_id}/contacts/{id}/topics - Update contact topics
  - target: "$.paths['/audiences/{audience_id}/contacts/{id}/topics'].patch"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # =============================================================================
  # Broadcast Operations
  # =============================================================================
  # GET /broadcasts - List broadcasts (read-only)
  - target: "$.paths['/broadcasts'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # POST /broadcasts - Create broadcast
  - target: "$.paths['/broadcasts'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: false
          openWorldHint: false

  # GET /broadcasts/{id} - Get broadcast (read-only)
  - target: "$.paths['/broadcasts/{id}'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # PATCH /broadcasts/{id} - Update broadcast
  - target: "$.paths['/broadcasts/{id}'].patch"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # DELETE /broadcasts/{id} - Delete broadcast (destructive)
  - target: "$.paths['/broadcasts/{id}'].delete"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: true
          idempotentHint: true
          openWorldHint: false

  # POST /broadcasts/{id}/send - Send broadcast (sends actual emails)
  - target: "$.paths['/broadcasts/{id}/send'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: false
          openWorldHint: true

  # =============================================================================
  # Webhook Operations
  # =============================================================================
  # GET /webhooks - List webhooks (read-only)
  - target: "$.paths['/webhooks'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # POST /webhooks - Create webhook
  - target: "$.paths['/webhooks'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: false
          openWorldHint: false

  # GET /webhooks/{webhook_id} - Get webhook (read-only)
  - target: "$.paths['/webhooks/{webhook_id}'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # PATCH /webhooks/{webhook_id} - Update webhook
  - target: "$.paths['/webhooks/{webhook_id}'].patch"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # DELETE /webhooks/{webhook_id} - Delete webhook (destructive)
  - target: "$.paths['/webhooks/{webhook_id}'].delete"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: true
          idempotentHint: true
          openWorldHint: false

  # =============================================================================
  # Segment Operations
  # =============================================================================
  # GET /segments - List segments (read-only)
  - target: "$.paths['/segments'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # POST /segments - Create segment
  - target: "$.paths['/segments'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: false
          openWorldHint: false

  # GET /segments/{id} - Get segment (read-only)
  - target: "$.paths['/segments/{id}'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # DELETE /segments/{id} - Delete segment (destructive)
  - target: "$.paths['/segments/{id}'].delete"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: true
          idempotentHint: true
          openWorldHint: false

  # =============================================================================
  # Topic Operations
  # =============================================================================
  # GET /topics - List topics (read-only)
  - target: "$.paths['/topics'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # POST /topics - Create topic
  - target: "$.paths['/topics'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: false
          openWorldHint: false

  # GET /topics/{id} - Get topic (read-only)
  - target: "$.paths['/topics/{id}'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # PATCH /topics/{id} - Update topic
  - target: "$.paths['/topics/{id}'].patch"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # DELETE /topics/{id} - Delete topic (destructive)
  - target: "$.paths['/topics/{id}'].delete"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: true
          idempotentHint: true
          openWorldHint: false

  # =============================================================================
  # Contact Property Operations
  # =============================================================================
  # GET /contact-properties - List contact properties (read-only)
  - target: "$.paths['/contact-properties'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # POST /contact-properties - Create contact property
  - target: "$.paths['/contact-properties'].post"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: false
          openWorldHint: false

  # GET /contact-properties/{id} - Get contact property (read-only)
  - target: "$.paths['/contact-properties/{id}'].get"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: true
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # PATCH /contact-properties/{id} - Update contact property
  - target: "$.paths['/contact-properties/{id}'].patch"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: false
          idempotentHint: true
          openWorldHint: false

  # DELETE /contact-properties/{id} - Delete contact property (destructive)
  - target: "$.paths['/contact-properties/{id}'].delete"
    update:
      x-speakeasy-mcp:
        annotations:
          readOnlyHint: false
          destructiveHint: true
          idempotentHint: true
          openWorldHint: false
